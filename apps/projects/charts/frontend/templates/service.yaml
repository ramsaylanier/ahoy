apiVersion: v1
kind: Service
metadata:
  name: { { include "ahoy-projects-frontend.fullname" . } }
  labels: { { - include "ahoy-projects-frontend.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector:
    { { - include "ahoy-projects-frontend.selectorLabels" . | nindent 4 } }
